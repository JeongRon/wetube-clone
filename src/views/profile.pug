extends base
include mixins/video.pug


block content
    div.profile__data
        if !user.avatarUrl
            span.icon 😀
        else if user.avatarUrl[0]==="h"
            img(src= user.avatarUrl,crossorigin).avatar
        else 
            img(src="/" + user.avatarUrl,crossorigin).avatar
        div
            h3=user.name
    div.video-grid
        each video in user.videos
            +video(video)
        else
            span.empty__message No videos found